version: 1.0.0
channel: incubating
description: Keycloak OIDC client in a tenant realm via Crossplane provider-keycloak.
category: identity

# Published as a signed OCI artifact: ghcr.io/7k-inari/catalog/keycloak-client (see docs/release-process.md).
