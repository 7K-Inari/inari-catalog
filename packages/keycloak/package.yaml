version: 1.0.0
channel: stable
type: platform-app
target: platform
description: >-
  Keycloak IAM for the platform cluster: Inari SSO, Organizations per tenant,
  OIDC issuer for agent/control-plane identity.
category: platform

# Published as a signed OCI artifact: ghcr.io/7k-inari/catalog/keycloak (see docs/release-process.md).
