apiVersion: apps/v1
kind: Deployment
metadata:
  name: violating-no-tenant-label
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: x
  template:
    metadata:
      labels:
        app: x
    spec:
      containers:
        - name: app
          image: nginx:1.27
