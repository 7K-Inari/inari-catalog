apiVersion: v1
kind: Pod
metadata:
  name: violating-privileged
  namespace: default
spec:
  containers:
    - name: app
      image: nginx:1.27
      securityContext:
        privileged: true
      resources:
        requests:
          cpu: 50m
          memory: 64Mi
        limits:
          memory: 128Mi
