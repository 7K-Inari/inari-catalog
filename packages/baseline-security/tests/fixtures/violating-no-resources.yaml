apiVersion: v1
kind: Pod
metadata:
  name: violating-no-resources
  namespace: default
spec:
  containers:
    - name: app
      image: nginx:1.27
      securityContext:
        readOnlyRootFilesystem: true
