apiVersion: v1
kind: Pod
metadata:
  name: violating-latest
  namespace: default
spec:
  containers:
    - name: app
      image: nginx:latest
      securityContext:
        readOnlyRootFilesystem: true
      resources:
        requests:
          cpu: 50m
          memory: 64Mi
        limits:
          memory: 128Mi
